<h1>Games</h1>

<div class="center">
<%= link_to "Show all", "/index?showall=true" %> ||
<%= link_to "Price: Low to High", "/index?order=ascending" %> ||
<%= link_to "Price: High to Low", "/index?order=descending" %>
</div>
<% @products.each do |product| %>
<div class="col-md-3">
  <a href="/index/<%= product.id %>" class="nil">  
  <div class="product-box">  
    <img class="center-block img-responsive" src="<%= product.images.first.image_url %>">
    <h4><%= product.name %></h4>
    <h5>Lowest Price: <%= number_to_currency(product.price) %></h5>
    <p class="center"><%= product.stock %></p>
    <div class="description_scroll"><%= product.description %></div>
  </div>
    </a>
</div>
<% end %>
</div>

<div class="button">
  <%= button_to 'Add Game', new_path, :class => 'btn btn-add btn-default btn-large' %>
</div>


